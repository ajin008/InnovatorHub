import React from "react";

const view = ({ id }: { id: string }) => {
  return (
    <div className="view-container">
      <div class name {
        constructor(parameters) {
          
        }
      }></div>
    </div>
  );
};

export default view;
